#
# ~/.bash_profile
#

[[ -f ~/.bashrc ]] && . ~/.bashrc

unset SSH_AGENT_PID

PATH="${HOME}/.bin:${HOME}/.local/bin:${PATH}"

export SSH_AUTH_SOCK="$(gpgconf --list-dirs agent-ssh-socket)"
export GPG_TTY=$(tty)
